angular.module("ModuleHotLine").controller("GridEditorCtrl", ["$scope", "$timeout", "$window",  
    function ($scope,$timeout, $window) {

		
		$scope.handleChange = function(selected, dataItem, data){
		};		
	
        $scope.init = function(options){
            console.log(options);
        };


    }]);
	
	
	